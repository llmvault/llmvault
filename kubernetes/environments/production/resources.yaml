apiVersion: v1
kind: LimitRange
metadata:
  name: production-defaults
  labels:
    hivy.io/apply-phase: foundation
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 128Mi
      default:
        cpu: "2"
        memory: 4Gi

