# Grandfathered TypeScript/TSX files exempted from the 600-line ceiling
# enforced by scripts/check-ts-file-length.sh.
#
# Hand-written files should be split instead of added here.
#
# Do NOT add new entries without explicit review — the default answer is
# "split the file", not "allowlist it".

# Existing chat workspace files predate the TypeScript length gate. Split in a
# dedicated frontend refactor; keep CI focused on new growth meanwhile.
apps/web/app/w/(chat)/_components/shell.tsx
apps/web/app/w/(chat)/_lib/session-tools.ts

# Existing session runtime store test is unchanged from main and predates the
# length gate. Keep it explicit until its broad store suite is split by domain.
apps/web/app/w/(chat)/_stores/session-runtime-store.test.ts
