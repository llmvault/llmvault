# Pinned to 1.96.0: the CI sandbox runtime job started getting OOM-cancelled
# the day 1.96.1 shipped (2026-06-30) with no repo change. Unpin once resolved.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
