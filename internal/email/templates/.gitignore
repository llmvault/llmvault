node_modules/
.react-email/
out/
*.log
*.tsbuildinfo

# dist/ holds the rendered HTML/text/manifest the Go backend embeds via
# //go:embed. The repo-root .gitignore ignores every `dist/`, so re-include
# this one explicitly — these files MUST be committed for `go build` to work.
!dist/
!dist/**
