// Package embedder contains the embedding-model clients (Qwen3-Embedding-4B
// via SiliconFlow by default, with a deterministic in-memory fake for
// tests).
//
// It defines the embedder interface and its implementations.
package embedder
